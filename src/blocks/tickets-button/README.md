# Tickets Button

Enables the "Get Tickets" button that dynamically selects the link based on the user's selection and the set ACF fields. It is dynamically injected into the editor (see [`src/editor/injectTicketsButton.ts`](/src/editor/injectTicketsButton.ts)).
