# Add To Calendar

1. Uses a fetch to get the calendar details (see [`inc/class-rest-router.php`](/inc/class-rest-router.php) and [`inc/Jobs/class-rest-data-formatter.php`](/inc/Jobs/class-rest-data-formatter.php))
2. Hooks into the `GET_TICKETS_STORE` to see if a user has selected a particular ticketed event time
